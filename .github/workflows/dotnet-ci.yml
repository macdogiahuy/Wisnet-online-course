name: .NET CI

on:
  push:
    branches: [upgrade-to-NET8, main]
  pull_request:
    branches: [upgrade-to-NET8, main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore CourseHub.sln

      - name: Build solution
        run: dotnet build CourseHub.sln --no-restore --configuration Release

      - name: Run tests
        run: dotnet test CourseHub.sln --no-build --configuration Release
